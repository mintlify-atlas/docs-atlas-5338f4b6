---
title: "Code scanning"
description: "Starter workflows for integrating static analysis, container security, dependency scanning, and infrastructure-as-code security tools into your CI pipeline."
---

Code scanning workflows analyze your code, container images, and infrastructure configuration for security vulnerabilities and coding errors. Results from all tools that produce SARIF output appear in the **Security** tab of your repository under **Code scanning alerts**, giving you a unified view regardless of which tools you use.

<Note>
  Templates under active development may carry `"labels": ["preview"]` in their
  `properties.json`, which hides them from the public template picker. You can
  still view and test preview templates by appending `?preview=true` to the new
  workflow page URL in any repository you own.
</Note>

## Tools by category

<Tabs>
  <Tab title="SAST">
    Static application security testing (SAST) tools analyze source code without executing it, looking for security vulnerabilities, coding errors, and policy violations.

    | Template file | Tool | Notes |
    |---|---|---|
    | `codeql.yml` | GitHub CodeQL | Semantic analysis for C/C++, C#, Go, Java/Kotlin, JavaScript/TypeScript, Python, Ruby, Swift |
    | `semgrep.yml` | Semgrep | Pattern-based analysis; requires a free Semgrep account |
    | `snyk-security.yml` | Snyk | Open-source vulnerability and license scanning |
    | `sonarcloud.yml` | SonarCloud | Cloud-hosted SonarQube analysis |
    | `sonarqube.yml` | SonarQube | Self-hosted SonarQube analysis |
    | `checkmarx-one.yml` | Checkmarx One | Enterprise SAST platform |
    | `checkmarx.yml` | Checkmarx SAST | Legacy Checkmarx SAST |
    | `bandit.yml` | Bandit | Python-specific security linter |
    | `brakeman.yml` | Brakeman | Ruby on Rails security scanner |
    | `bearer.yml` | Bearer | Data-flow security analysis |
    | `codacy.yml` | Codacy | Multi-language static analysis |
    | `codescan.yml` | CodeScan | Salesforce-specific code quality |
    | `contrast-scan.yml` | Contrast Scan | IAST and SAST analysis |
    | `credo.yml` | Credo | Elixir static analysis |
    | `detekt.yml` | Detekt | Kotlin static analysis |
    | `devskim.yml` | DevSkim | IDE and pipeline security linter by Microsoft |
    | `eslint.yml` | ESLint security rules | JavaScript/TypeScript linting with security plugins |
    | `flawfinder.yml` | Flawfinder | C/C++ security analysis |
    | `fortify.yml` | OpenText Fortify | Enterprise SAST |
    | `jfrog-sast.yml` | JFrog SAST | JFrog Advanced Security static analysis |
    | `lintr.yml` | lintr | R static analysis |
    | `msvc.yml` | MSVC Code Analysis | Microsoft Visual C++ static analysis |
    | `njsscan.yml` | njsscan | Node.js security scanning |
    | `ossar.yml` | OSSAR | Open Source Static Analysis Runner (multiple tools) |
    | `phpmd.yml` | PHPMD | PHP Mess Detector |
    | `pmd.yml` | PMD | Java/Apex static analysis |
    | `powershell.yml` | PSScriptAnalyzer | PowerShell security analysis |
    | `psalm.yml` | Psalm | PHP static analysis |
    | `puppet-lint.yml` | puppet-lint | Puppet manifest linting |
    | `pyre.yml` | Pyre | Python type checker with security extensions |
    | `pysa.yml` | Pysa | Python taint analysis |
    | `rubocop.yml` | RuboCop | Ruby static analysis and linting |
    | `rust-clippy.yml` | Clippy | Rust static analysis |
    | `securitycodescan.yml` | Security Code Scan | .NET security analysis |
    | `sobelow.yml` | Sobelow | Elixir/Phoenix security analysis |
    | `veracode.yml` | Veracode | Enterprise SAST and SCA |
    | `xanitizer.yml` | Xanitizer | Java/Kotlin security analysis |
    | `clj-holmes.yml` | clj-holmes | Clojure security analysis |
    | `clj-watson.yml` | clj-watson | Clojure dependency vulnerability scanning |
    | `synopsys-action.yml` | Synopsys | Black Duck and Coverity via Synopsys Bridge |
    | `synopsys-io.yml` | Synopsys IO | Intelligent orchestration for Synopsys tools |
  </Tab>
  <Tab title="Container security">
    Container security tools scan Docker images and Kubernetes manifests for known vulnerabilities in OS packages and application dependencies.

    | Template file | Tool | Notes |
    |---|---|---|
    | `trivy.yml` | Trivy | Scans container images for OS and library vulnerabilities |
    | `snyk-container.yml` | Snyk Container | Container image vulnerability scanning |
    | `anchore.yml` | Anchore Enterprise | Enterprise container vulnerability scanning |
    | `anchore-syft.yml` | Anchore Syft | SBOM generation for container images |
    | `prisma.yml` | Prisma Cloud | Container and cloud security |
    | `sysdig-scan.yml` | Sysdig | Runtime and image vulnerability scanning |
    | `nowsecure.yml` | NowSecure | Mobile application security testing |
    | `nowsecure-mobile-sbom.yml` | NowSecure SBOM | Mobile app SBOM generation |
    | `mobsf.yml` | MobSF | Mobile Security Framework analysis |
    | `appknox.yml` | Appknox | Mobile application security testing |
  </Tab>
  <Tab title="Dependency scanning">
    Dependency scanning tools identify known vulnerabilities in your open-source dependencies and generate software bill of materials (SBOM) reports.

    | Template file | Tool | Notes |
    |---|---|---|
    | `dependency-review.yml` | Dependency Review | GitHub-native action; blocks PRs that introduce vulnerable dependencies |
    | `osv-scanner.yml` | OSV Scanner | Google's open-source vulnerability scanner |
    | `debricked.yml` | Debricked | Open-source security and license compliance |
    | `endorlabs.yml` | Endor Labs | Reachability-based SCA |
    | `frogbot-scan-pr.yml` | JFrog Frogbot | Scan pull requests for dependency vulnerabilities |
    | `frogbot-scan-and-fix.yml` | JFrog Frogbot | Scan and auto-fix dependency vulnerabilities |
    | `scorecard.yml` | OpenSSF Scorecard | Supply chain security posture scoring |
    | `crda.yml` | Red Hat CRDA | Red Hat CodeReady Dependency Analytics |
  </Tab>
  <Tab title="Infrastructure as Code">
    IaC scanning tools analyze Terraform, CloudFormation, Kubernetes, and Dockerfile configurations for security misconfigurations before they reach production.

    | Template file | Tool | Notes |
    |---|---|---|
    | `tfsec.yml` | tfsec | Terraform static analysis |
    | `policy-validator-tf.yaml` | AWS Policy Validator (Terraform) | Validate IAM policies in Terraform configurations |
    | `policy-validator-cfn.yaml` | AWS Policy Validator (CloudFormation) | Validate IAM policies in CloudFormation templates |
    | `kubesec.yml` | kubesec | Kubernetes resource security scoring |
    | `zscaler-iac-scan.yml` | Zscaler IaC Scan | Multi-cloud IaC scanning |
    | `datree.yml` | Datree | Kubernetes configuration validation |
    | `hadolint.yml` | Hadolint | Dockerfile linting |
    | `snyk-infrastructure.yml` | Snyk Infrastructure as Code | IaC vulnerability and misconfiguration scanning |
    | `cloudrail.yml` | Cloudrail | Context-aware Terraform security analysis |
  </Tab>
  <Tab title="API & Web security">
    These tools test running API endpoints and web applications for security vulnerabilities.

    | Template file | Tool | Notes |
    |---|---|---|
    | `stackhawk.yml` | StackHawk | Dynamic API and web application security testing |
    | `neuralegion.yml` | Bright Security | DAST for APIs and web apps |
    | `soos-dast-scan.yml` | SOOS DAST | Dynamic application security testing |
    | `apisec-scan.yml` | APIsec | API security testing |
    | `ethicalcheck.yml` | EthicalCheck | API security scanning |
    | `mayhem-for-api.yml` | Mayhem for API | Fuzz testing for APIs |
    | `crunch42.yml` | Crunch42 | API security scanning |
    | `jscrambler-code-integrity.yml` | Jscrambler | JavaScript code integrity and obfuscation |
  </Tab>
</Tabs>

---

## Example: CodeQL Advanced

CodeQL is GitHub's own semantic code analysis engine. The `codeql.yml` template runs on push, pull request, and a weekly schedule, using a language matrix so a single workflow file covers all languages in your repository.

<Accordion title="codeql.yml — full workflow">
```yaml
# For most projects, this workflow file will not need changing; you simply need
# to commit it to your repository.
#
# You may wish to alter this file to override the set of languages analyzed,
# or to provide custom queries or build logic.
#
# ******** NOTE ********
# We have attempted to detect the languages in your repository. Please check
# the `language` matrix defined below to confirm you have the correct set of
# supported CodeQL languages.
#
name: "CodeQL Advanced"

on:
  push:
    branches: [ $default-branch, $protected-branches ]
  pull_request:
    branches: [ $default-branch, $protected-branches ]
  schedule:
    - cron: $cron-weekly

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    # Runner size impacts CodeQL analysis time. To learn more, please see:
    #   - https://gh.io/recommended-hardware-resources-for-running-codeql
    #   - https://gh.io/supported-runners-and-hardware-resources
    #   - https://gh.io/using-larger-runners (GitHub.com only)
    # Consider using larger runners or machines with greater resources for possible analysis time improvements.
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      # required for all workflows
      security-events: write

      # required to fetch internal or private CodeQL packs
      packages: read

      # only required for workflows in private repositories
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        $codeql-languages-matrix
        # CodeQL supports the following values keywords for 'language': $supported-codeql-languages
        # Use `c-cpp` to analyze code written in C, C++ or both
        # Use 'java-kotlin' to analyze code written in Java, Kotlin or both
        # Use 'javascript-typescript' to analyze code written in JavaScript, TypeScript or both
        # To learn more about changing the languages that are analyzed or customizing the build mode for your analysis,
        # see https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/customizing-your-advanced-setup-for-code-scanning.
        # If you are analyzing a compiled language, you can modify the 'build-mode' for that language to customize how
        # your codebase is analyzed, see https://docs.github.com/en/code-security/code-scanning/creating-an-advanced-setup-for-code-scanning/codeql-code-scanning-for-compiled-languages
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # Add any setup steps before running the `github/codeql-action/init` action.
    # This includes steps like installing compilers or runtimes (`actions/setup-node`
    # or others). This is typically only required for manual builds.
    # - name: Setup runtime (example)
    #   uses: actions/setup-example@v1

    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
        # If you wish to specify custom queries, you can do so here or in a config file.
        # By default, queries listed here will override any specified in a config file.
        # Prefix the list here with "+" to use these queries and those in the config file.

        # For more details on CodeQL's query packs, refer to: https://docs.github.com/en/code-security/code-scanning/automatically-scanning-your-code-for-vulnerabilities-and-errors/configuring-code-scanning#using-queries-in-ql-packs
        # queries: security-extended,security-and-quality

    # If the analyze step fails for one of the languages you are analyzing with
    # "We were unable to automatically build your code", modify the matrix above
    # to set the build mode to "manual" for that language. Then modify this step
    # to build your code.
    - name: Run manual build steps
      if: matrix.build-mode == 'manual'
      shell: bash
      run: |
        echo 'If you are using a "manual" build mode for one or more of the' \
          'languages you are analyzing, replace this with the commands to build' \
          'your code, for example:'
        echo '  make bootstrap'
        echo '  make release'
        exit 1

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"
```
</Accordion>

Key points in the template:

- **`$codeql-languages-matrix`** — a placeholder replaced by GitHub with a matrix entry for each language detected in your repository.
- **`fail-fast: false`** — ensures that a build failure for one language does not cancel analysis of other languages.
- **`security-events: write`** — required by `github/codeql-action/analyze` to upload SARIF results to the Security tab.
- **Swift runs on `macos-latest`** — CodeQL requires macOS to analyze Swift code.

---

## Example: Trivy container scanner

Trivy scans a Docker image for `CRITICAL` and `HIGH` severity vulnerabilities and uploads results as SARIF to the Security tab.

<Accordion title="trivy.yml — full workflow">
```yaml
# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: trivy

on:
  push:
    branches: [ $default-branch, $protected-branches ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ $default-branch ]
  schedule:
    - cron: $cron-weekly

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: read # for actions/checkout to fetch code
      security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
      actions: read # only required for a private repository by github/codeql-action/upload-sarif to get the Action run status
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build an image from Dockerfile
        run: |
          docker build -t docker.io/my-organization/my-app:${{ github.sha }} .

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@7b7aa264d83dc58691451798b4d117d53d21edfe
        with:
          image-ref: 'docker.io/my-organization/my-app:${{ github.sha }}'
          format: 'template'
          template: '@/contrib/sarif.tpl'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'
```
</Accordion>

Scanning results are uploaded with `github/codeql-action/upload-sarif` and appear under **Security → Code scanning alerts**. The pinned action SHA (`@7b7aa264d83dc58691451798b4d117d53d21edfe`) is a security best practice for third-party actions — it prevents a compromised tag from silently changing the code you run.
